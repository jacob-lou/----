# 热点追踪系统 — 方案迭代日志

> 记录每次架构变更的背景、决策和实现细节。  
> 最新条目在最前面。

---

## V1.5 — Agent Skill 封装（热点监控工具独立技能包）

**日期**：2026-04-02  
**状态**：已完成

### 背景

V1.4 完成多语言翻译后，系统已具备完整的 10 源采集 + 分类 + 翻译 + 重要度排序 + AI 分析 + 关键词监控能力。但这些能力被绑定在 Express 服务端，必须启动完整服务才能使用。需求：

1. **让 AI Agent 直接使用采集能力**：用户在 Copilot/Claude 中提问"最近 AI 有什么热点"时，Agent 能立即执行脚本获取实时数据
2. **零配置门槛**：不需要启动服务、不需要数据库、不需要 API Key（Twitter 除外）
3. **不影响现有项目代码**：纯增量，仅新增 `skills/` 目录

### 核心设计：Claude 即 AI 引擎

**关键创新**：脚本只负责抓取原始数据，所有智能分析（分类、翻译、重要度判断、趋势总结）由 AI Agent 自身完成。

```
旧模式: 脚本抓数据 → 调 OpenRouter 分类 → 调 OpenRouter 翻译 → 调 OpenRouter 分析
新模式: 用户提问 → Agent 读 SKILL.md → Agent 调脚本抓原始数据 → Agent 自身分析 → Agent 输出报告
```

**优势**：
- 零 API Key 配置 — 脚本只做纯 HTTP 数据抓取
- 零额外 AI 费用 — 分析由用户已付费的 Agent 模型完成
- 分析质量跟随模型 — 用 Opus 就是 Opus 级别分析

### 技术方案

#### 1. 职责划分

| 层 | 职责 | 实现 |
|---|------|------|
| 数据层（脚本） | 抓取原始数据、输出 JSON | TypeScript 纯函数，fetch + cheerio |
| 智能层（Agent） | 分类、翻译、重要度、质量判断、报告生成 | SKILL.md 中的分析框架 |
| 交互层（用户） | 自然语言提问 | "最近 AI 有什么热点" |

#### 2. 目录结构

```
skills/hot-topics-monitor/
├── SKILL.md                       # 主入口：触发条件 + 工作流 + 分析框架
├── scripts/
│   ├── fetch-trends.ts            # 多源热点采集（输出 JSON）
│   ├── search-keyword.ts          # 跨源关键词搜索（输出 JSON）
│   └── lib/
│       ├── sources/               # 10 个数据源纯函数
│       │   ├── google.ts          # Google Trends RSS
│       │   ├── reddit.ts          # Reddit 11 个 AI subreddit
│       │   ├── hackernews.ts      # HN Algolia API
│       │   ├── duckduckgo.ts      # DDG 补全 + 即时答案
│       │   ├── github.ts          # GitHub Trending HTML
│       │   ├── huggingface.ts     # HF 模型 + 论文 API
│       │   ├── v2ex.ts            # V2EX 热帖 API
│       │   ├── bingnews.ts        # Bing News RSS
│       │   ├── bilibili.ts        # B站热搜 + 科技区
│       │   └── twitter.ts         # Twitter API（需 Key）
│       ├── types.ts               # TrendData, SourceResult 等类型
│       └── utils.ts               # fetchWithTimeout, parseArgs 等工具
└── references/
    ├── architecture.md            # 系统架构 + Prisma Schema + REST API + WebSocket
    ├── data-sources.md            # 10 个数据源详细文档
    └── extending.md               # 扩展指南 + 新增数据源/监控示例
```

#### 3. 脚本设计

**fetch-trends.ts** — 多源热点采集：
```bash
npx tsx skills/hot-topics-monitor/scripts/fetch-trends.ts [--sources google,reddit] [--limit 20]
```
- 10 个数据源并行采集，输出 JSON 到 stdout
- 每个源独立 try/catch，单源失败不影响其他
- Twitter 无 API Key 时自动跳过，stderr 输出提示

**search-keyword.ts** — 跨源关键词搜索：
```bash
npx tsx skills/hot-topics-monitor/scripts/search-keyword.ts "GPT-5" [--sources reddit,hackernews]
```
- 跨 Reddit/HackerNews/DuckDuckGo/Twitter 搜索
- 所有搜索 10 秒超时

#### 4. SKILL.md 分析框架

SKILL.md 不仅是触发入口，更是 Agent 的分析操作手册：

- **工作流决策树**：用户意图 → 选择脚本 → 执行 → 分析 → 输出
- **分类框架**：内置 60+ AI 关键词库 + 语义判断规则
- **重要度评估**：四因子公式（热度 35% + 互动 25% + 跨源 25% + 时效 15%）
- **质量判断准则**：识别标题党、广告、搬运、过时内容
- **报告模板**：按重要性分级的结构化 Markdown 输出格式

#### 5. 数据源提取策略

从 `src/sources/*.ts` 提取为纯函数版本：
- 去掉 class 包装和 `TrendSource` 接口依赖
- 去掉 Prisma/Express/Socket.IO 依赖
- 保留核心采集逻辑、过滤策略、去重机制
- 唯一外部依赖：cheerio（项目已安装，用于 HTML/XML 解析）

### 验证结果

| 测试 | 结果 |
|------|------|
| `fetch-trends.ts --sources hackernews --limit 3` | ✅ 3 条 HN 热点，JSON 格式正确 |
| `search-keyword.ts "DeepSeek" --sources hackernews,duckduckgo` | ✅ 21 条跨源结果 |
| `fetch-trends.ts --sources v2ex --limit 3` | ✅ 中文源正常工作 |
| `fetch-trends.ts --sources twitter` | ✅ 无 API Key 优雅跳过，不崩溃 |

### 涉及文件

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `skills/hot-topics-monitor/SKILL.md` | **新建** | 主入口：触发条件 + 分析框架 + 报告模板 |
| `skills/hot-topics-monitor/scripts/fetch-trends.ts` | **新建** | 多源热点采集 CLI |
| `skills/hot-topics-monitor/scripts/search-keyword.ts` | **新建** | 跨源关键词搜索 CLI |
| `skills/hot-topics-monitor/scripts/lib/types.ts` | **新建** | 共享类型定义 |
| `skills/hot-topics-monitor/scripts/lib/utils.ts` | **新建** | 工具函数（超时、参数解析） |
| `skills/hot-topics-monitor/scripts/lib/sources/*.ts` (10) | **新建** | 10 个数据源纯函数采集逻辑 |
| `skills/hot-topics-monitor/references/architecture.md` | **新建** | 系统架构 + Schema + API 文档 |
| `skills/hot-topics-monitor/references/data-sources.md` | **新建** | 10 个数据源详细文档 |
| `skills/hot-topics-monitor/references/extending.md` | **新建** | 扩展指南 + 示例 |

共 18 个新文件，0 个修改文件（不影响现有代码）。

### 已知遗留

- Phase 2 待实施：趋势方向箭头（scoreDelta）、话题自动折叠分组、AI 价值标签
- Skill 描述优化：可用 Skill Creator 的 description optimization 流程进一步提升触发准确率
- 可考虑添加更多搜索源到 search-keyword.ts（如 GitHub Code Search、Google News）

---

## V1.4 — 多语言翻译 + 信息增强 + 全站 i18n（Phase 1）

**日期**：2026-04-01 ~ 2026-04-02  
**状态**：已完成

### 背景

V1.3 完成重要度排序后，日常使用中发现三个痛点：

1. **标题语言混杂**：10 个数据源涵盖英/中/日/韩文，用户需逐条理解外语标题才能判断内容价值，效率低下
2. **信息密度不足**：只有标题、分数、来源和时间，缺少摘要、跨源热度等辅助判断信息
3. **界面纯中文**：无法切换为英文界面

### 目标

- 自动检测标题语言，翻译为中英双语
- 添加语言标签、跨源热议徽章、一键摘要
- 全站 i18n 支持中/英切换（包括所有 UI 文本）
- 默认筛选优化：最近 7 天 + 高热度 + 每页 20 条

### 技术方案

#### 1. 数据层变更

```prisma
model TrendItem {
  // ... 现有字段
  language         String?              // 检测到的源语言：zh/en/ja/ko
  titleZh          String?              // 中文翻译标题
  titleEn          String?              // 英文翻译标题
  summary          String?              // AI 生成的一句话摘要（≤30字）
  crossSourceCount Int    @default(0)   // 跨源出现次数（由 importance.ts 计算）
  @@index([language])
}
```

#### 2. 翻译服务

新建 `src/services/translator.ts`，核心功能：

**语言检测**（零成本，本地启发式）：
- Unicode 范围判定：CJK Unified (U+4E00–U+9FFF) → zh，Hiragana/Katakana → ja，Hangul → ko
- 英文回退：非 CJK 字符占多数时判定为 en
- 混合文本按字符比例投票

**批量翻译**（OpenRouter API）：
- 英文源 → 仅生成 titleZh；中文源 → 仅生成 titleEn；日/韩 → 生成两者
- 每批 ≤50 条，temperature 0.1 保持一致性
- Prompt 要求仅翻译、不解释，返回 JSON 数组
- 降级策略：翻译失败 → 仅保留语言检测结果，不影响入库

**摘要生成**（按需，非自动）：
- `POST /api/trends/summary` — 前端用户点击「生成摘要」时调用
- 批量处理（≤50 条），每条生成 ≤30 字中文摘要
- 已有摘要直接返回缓存，不重复调用 API

**历史数据回填**：
- `POST /api/trends/backfill-translations` — 扫描 `language IS NULL` 的条目
- 每批 100 条，自动检测+翻译，后台异步执行

#### 3. 采集流程变更

```
Before:  fetch → classify → save
After:   fetch → classify → detectAndTranslate → save (with language/titleZh/titleEn)
```

- `collector.ts` 集成 `TranslatorService`
- `detectAndTranslate(items)` 先 `detectLanguage()` 再按需翻译
- 翻译失败时降级为仅语言检测（`detectLanguage` fallback）

#### 4. 跨源计数集成

`importance.ts` 的 `computeScores()` 在计算跨源匹配时，同时输出 `crossSourceCount` 写入 DB：

```typescript
const crossSourceCount = new Map<number, number>()
// ... tokenize + 倒排索引匹配后
crossSourceCount.set(item.id, matchingSources.size)
// ... batch update 时一并写入
data: { importanceScore: score, crossSourceCount: count }
```

#### 5. 全站 i18n 系统

前端 `app.js` 新增完整的中英翻译框架：

```javascript
var i18n = {
  zh: { appTitle: '热点追踪', connected: '已连接', ... /* 80+ keys */ },
  en: { appTitle: 'Hot Topics', connected: 'Connected', ... /* 80+ keys */ }
};
function t(key) { return (i18n[displayLang] || i18n.zh)[key] || i18n.zh[key] || key; }
```

**覆盖范围**：
- 顶栏：应用名、AI 分析按钮、刷新按钮 title、连接状态
- Tab 标签：AI 热点 / 综合热点 / 热点监控
- 工具栏：搜索占位符、排序选项、数据源、筛选按钮
- 筛选面板：时间/热度/选项标签、所有 pill 按钮、全选、每页、紧凑
- 趋势列表：空状态、加载失败、摘要按钮（生成摘要/生成中/失败/N/A）
- 分页：上一页/下一页
- AI 分析面板：标题、分析中状态
- 跨源徽章：2源热议/3源热议/4+源热议
- 热点监控：输入框占位符、添加/检查/采集按钮、告警/热点标签、排序选项、时间筛选、最近告警标题
- 关键词卡片：告警/热点计数、暂停/启用/删除
- Socket.IO：已连接/断开/新数据、采集状态、关键词告警
- 时间显示：刚刚/分钟前/小时前/天前

**`applyUILanguage()` 函数**：切换语言时遍历所有 DOM 元素更新文本，保留 SVG 图标。切换语言同时重新加载当前 tab 数据（触发 `getDisplayTitle` 切换标题语言）。

#### 6. 默认筛选优化

| 配置项 | 旧值 | 新值 | 原因 |
|--------|------|------|------|
| 时间范围 | 30 天 | 7 天 | 热点有时效性，默认看最近一周 |
| 热度门槛 | 不限 | ≥200 | 过滤低质量，聚焦有价值内容 |
| 每页条数 | 50 | 20 | 减少每页信息量，降低认知负荷 |

JS 状态初始值和 HTML pill `active` class / select `selected` 均同步更新。

#### 7. 摘要生成策略变更

- **移除**：`renderTrends()` 末尾的 `requestSummaries()` 自动批量调用
- **改为**：仅用户点击「生成摘要」按钮时触发单条/批量 API 调用
- **原因**：自动生成导致每次翻页触发 20+ 次 OpenRouter API 调用，浪费费用

### Backfill 结果

| 指标 | 值 |
|------|---|
| 总条目数 | 1,388 |
| 已检测语言 | 1,388 (100%) |
| 已翻译中文 | 1,210 条 |
| 已翻译英文 | 181 条 |
| 剩余未翻译 | 0 条 |

### 涉及文件

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `prisma/schema.prisma` | 修改 | TrendItem 加 language/titleZh/titleEn/summary/crossSourceCount + index |
| `src/services/translator.ts` | **新建** | 语言检测 + 批量翻译 + 摘要生成 + 历史回填 |
| `src/services/collector.ts` | 修改 | 集成 TranslatorService，采集流程加翻译步骤 |
| `src/services/importance.ts` | 修改 | computeScores 同时输出 crossSourceCount |
| `src/routes/trends.ts` | 修改 | 新增 POST /summary 和 POST /backfill-translations 端点 |
| `public/index.html` | 修改 | 语言下拉、默认筛选 pill/select 调整 |
| `public/app.js` | 修改 | i18n 系统（80+ key）、applyUILanguage()、getDisplayTitle/getOriginalTitle、默认状态、移除自动摘要 |
| `public/style.css` | 修改 | .lang-tag/.cross-source-badge/.trend-summary/.btn-summary/.lang-trigger 样式 |
| `src/__tests__/v1.4.test.ts` | **新建** | 18 个测试覆盖语言检测/标签/跨源/翻译显示逻辑 |

### 测试结果

59 个测试全部通过（41 existing + 18 new）。

### 已知遗留

- 14 个 TypeScript 编译警告（V1.2 起存在）：Express 5 params 类型 (4) + fetch().json() 返回 unknown (9) + test top-level await (1)，不影响运行
- Phase 2 待实施：趋势方向箭头（scoreDelta）、话题自动折叠分组、AI 价值标签

---

## V1.3 — 智能重要度排序 + 按筛选独立 AI 分析

**日期**：2026-03-31 ~ 2026-04-01  
**状态**：已完成

### 背景

V1.2 完成内容分类后，两个核心功能仍有缺陷：

1. **"重要程度"排序是假的**：仅按 `score DESC, commentsCount DESC` 三重排序，不同数据源的 score 量纲完全不同（Twitter ~20K-46K，Bilibili 视频 ~百万播放，GitHub ~几百 stars，HN ~几百分），导致 Twitter/Bilibili 永远排在最前面，跨源比较无意义。
2. **AI 分析两个 Tab 完全相同**：`loadAnalysis()` 获取全局唯一分析结果后同时渲染到 "AI 热点" 和 "综合热点" 两个面板，内容完全一致，没有针对各自板块和筛选条件做差异化分析。

### 目标

- 重要度排序：跨数据源可比较，综合热度、互动、多源交叉出现程度
- AI 分析：按当前 Tab 的 category + 所有筛选条件独立生成/缓存，筛选变化后自动触发（5s 防抖）

### 技术方案

#### 1. 重要度评分引擎

新建 `src/services/importance.ts`，采用**三因子组合评分**模型：

| 因子 | 权重 | 计算方式 |
|------|------|---------|
| 归一化热度 | 40% | 同源内 score 按百分位排名 → 0~100 |
| 归一化互动 | 30% | 同源内 commentsCount 按百分位排名 → 0~100 |
| 跨源频率 | 30% | 标题 token 倒排索引，检测多源重复话题 → 每匹配 1 源 +25，上限 100 |

**公式**：`importanceScore = normalizedScore × 0.4 + normalizedComments × 0.3 + crossSourceBonus × 0.3`

**标题 Tokenize 策略**：
- 英文：提取 ≥3 字符的单词，过滤 60+ 停用词（the, and, for…）
- 中文/日韩：CJK 字符串按 bigram 切分（"人工智能" → "人工", "工智", "智能"）
- 混合文本同时处理两种

**百分位归一化**：同源内按原始值排序，`percentile = rank / (n-1) × 100`，消除跨源量纲差异。

**评分触发时机**：
- 每次定时采集完成后自动计算
- 手动刷新后自动计算
- 首次部署 backfill 全量历史数据

#### 2. 数据层变更

```prisma
model TrendItem {
  // ... 现有字段
  importanceScore Float @default(0)       // 新增：预计算重要度分数 0~100
  @@index([importanceScore])              // 新增索引
}

model TrendAnalysis {
  // ... 现有字段
  category   String @default("all")       // 新增：分析所属分类
  filterHash String @default("")          // 新增：筛选条件 MD5 哈希（前 12 位）
  @@index([category, filterHash])         // 新增复合索引
}
```

#### 3. AI 分析按筛选条件独立

**后端**（`src/services/analysis.ts`）：

```typescript
interface AnalysisFilter {
  category?: string   // 'ai' | 'general' | 'all'
  source?: string     // 逗号分隔多源
  search?: string     // 搜索关键词
  days?: number       // 时间范围
  minScore?: number   // 热度门槛
}
```

- `analyzeTrends(filter)` 按筛选条件构建 Prisma where → 取 Top 80 条 → 发给 OpenRouter 分析
- Prompt 中注入分类标签（"AI/科技" / "综合" / "全部"），让 AI 理解分析范围
- `buildFilterHash(filter)` 将筛选条件序列化后取 MD5 前 12 位作缓存 key
- 缓存查询：`getLatestAnalysis(category, filterHash)` → 按 category + filterHash 查最新
- 过期策略：前端检测分析时间 > 1 小时则自动触发刷新

**API 变更**：
- `GET /api/trends/analysis?category=ai&source=reddit&days=7&...` → 返回匹配筛选条件的缓存分析
- `POST /api/trends/analyze` → body 中传入 `{ category, source, search, days, minScore }`

**前端**（`public/app.js`）：
- 新增 `scheduleAnalysis(category, state)` — 5s 防抖定时器
- `reloadAi()` / `reloadGeneral()` 每次调用后自动 schedule 对应 tab 的分析
- `loadAnalysisForTab(category, state)` — 按当前筛选条件请求缓存分析
- Socket `analysis-update` 按 `data.category` 路由到对应 tab 面板
- 手动 "AI 分析" 按钮自动识别当前 tab 发对应请求
- 分析加载中显示 "正在分析中…" 状态

#### 4. 排序逻辑变更

```diff
  case 'importance':
-   orderBy = [{ score: 'desc' }, { commentsCount: 'desc' }]
-   useRawImportance = true
+   orderBy = [{ importanceScore: 'desc' }, { fetchedAt: 'desc' }]
```

删除 `useRawImportance` 分支，统一走 Prisma `orderBy`。

#### 5. 定时任务变更

```
采集完成 → 计算重要度 → 分析 AI 分类 → 分析综合分类
```

Scheduler 在 `collectAll()` 后依次调用 `importance.computeScores()` 和两次 `analysis.analyzeTrends({ category })` → 通过 Socket 推送到各自 tab。

### Backfill 结果

| 指标 | 值 |
|------|---|
| 处理条目数 | 1,134 |
| 0-20 分 | 63 条 (5.6%) |
| 20-40 分 | 231 条 (20.4%) |
| 40-60 分 | 378 条 (33.3%) |
| 60-80 分 | 291 条 (25.7%) |
| 80-100 分 | 171 条 (15.1%) |

### 涉及文件

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `prisma/schema.prisma` | 修改 | TrendItem 加 importanceScore；TrendAnalysis 加 category/filterHash |
| `src/services/importance.ts` | **新建** | 三因子重要度评分引擎 |
| `src/services/analysis.ts` | 重写 | 支持 AnalysisFilter 参数 + filterHash 缓存 |
| `src/routes/trends.ts` | 修改 | 分析接口支持筛选参数；importance 排序改用 importanceScore |
| `src/scheduler.ts` | 修改 | 采集后计算重要度 + 分别分析两个分类 |
| `public/app.js` | 修改 | 防抖分析、按 tab 独立加载、Socket 路由 |
| `src/__tests__/v1.3.test.ts` | **新建** | 20 个测试覆盖 tokenize/归一化/跨源加分/公式/filterHash |

### 测试结果

41 个测试全部通过（21 existing + 20 new）。

---

## V1.2.1 — 四项 Bug 修复

**日期**：2026-03-31  
**状态**：已完成

### 修复内容

| # | 问题 | 修复方案 |
|---|------|---------|
| 1 | "最新" 和 "最新采集" 排序名称歧义 | "最新" → "最新发布"（按 publishedAt），"最新采集" 不变（按 fetchedAt） |
| 2 | 缺少 "重要程度" 排序选项 | 新增 `importance` 排序（当时临时用 score+comments 三重排序，V1.3 已替换） |
| 3 | "综合热点" Tab 缺少 AI 分析面板 | HTML 新增 `generalAnalysisPanel`，`renderAnalysis()` 支持 target 参数 |
| 4 | Bilibili 热搜 score 为 243K（误用 hot_id） | `hot_id` 是自增 ID，改为 `(100 - position) × 100`，修复 69 条历史记录 |

### 涉及文件

- `public/index.html` — 新增综合分析面板 HTML + 排序选项重命名
- `public/app.js` — `renderAnalysis()` 支持 general target
- `src/routes/trends.ts` — 新增 importance case
- `src/sources/bilibili.ts` — 热搜 score 计算修正
- `src/__tests__/fixes.test.ts` — 21 个测试覆盖全部 4 项修复

---

## V1.2 — 基于内容的智能分类系统

**日期**：2026-03-31  
**状态**：已完成

### 背景

V1.1 中 "AI 热点" 和 "综合热点" 的划分方式是**按数据源硬编码**（GitHub/HF/HN/Twitter/BingNews → AI，Google/Reddit/DDG/V2EX/Bilibili → 综合）。问题：

- Reddit 上的 AI 帖子（如 r/LocalLLaMA）被归到综合板块
- Bilibili 的 "谷歌AI内存压缩算法遭质疑" 热搜被归到综合板块
- Google Trends 偶尔出现 AI 相关趋势也无法进入 AI 板块
- 分类逻辑与内容无关，仅靠来源区分，不符合实际需求

### 目标

- 所有数据源的内容按**语义**分类，AI 相关内容无论来源都归入 "AI 热点"
- V1 固定 `ai` / `general` 两个板块
- 架构预留 V2 用户自定义板块（如 "区块链"、"游戏"、"政治"）

### 技术方案

#### 1. 数据层变更

```prisma
model TrendItem {
  // ... 现有字段
  category    String   @default("general")  // 新增：内容分类
  // ...
  @@index([category])                       // 新增索引
}

// V2 预留：分类规则表
model CategoryRule {
  id          Int      @id @default(autoincrement())
  name        String   @unique              // 板块名称，如 "ai", "blockchain"
  label       String                        // 显示名称，如 "AI 热点"
  keywords    String                        // JSON 数组：匹配关键词
  description String?                       // 板块描述
  isSystem    Boolean  @default(false)      // true=系统内置, false=用户自建
  priority    Int      @default(0)          // 匹配优先级（高优先）
  createdAt   DateTime @default(now())
}
```

#### 2. 分类引擎（混合模式）

新建 `src/services/classifier.ts`，两阶段分类：

```
采集完成 → Phase A: 关键词规则匹配 → Phase B: AI 模型判断 → 入库
              ↓ 命中                    ↓ 未命中
           category='ai'           发给 OpenRouter 批量判断
                                       ↓ API 不可用
                                   降级为 'general'
```

**Phase A — 关键词规则匹配**（零成本、零延迟）
- 扫描 `title` + `extra` 中的描述/摘要字段
- 预置 AI 关键词库（约 60+ 词），大小写不敏感：

| 类别 | 关键词示例 |
|------|-----------|
| 通用 | AI, artificial intelligence, 人工智能, machine learning, 机器学习, deep learning, 深度学习 |
| 模型 | LLM, GPT, Claude, Gemini, DeepSeek, 千问, Qwen, Llama, Mistral, BERT, transformer |
| 产品 | OpenAI, Anthropic, HuggingFace, Copilot, ChatGPT, Midjourney, Stable Diffusion, Sora |
| 技术 | AIGC, AGI, RAG, fine-tune, RLHF, LoRA, embedding, inference, tokenizer, neural network |
| 领域 | NLP, computer vision, 自动驾驶, 大模型, 开源模型, prompt engineering, AI agent |

- 命中任意关键词 → `category = 'ai'`，跳过 Phase B
- 匹配逻辑：单词边界匹配（英文）+ 包含匹配（中文），避免 "MAIL" 误命中 "AI"

**Phase B — AI 大模型批量判断**（仅处理 Phase A 未分类项）
- 将未分类项的标题批量发送给 OpenRouter（每批 ≤50 条）
- Prompt：返回 JSON 数组标注每条是 `"ai"` 还是 `"general"`
- 降级策略：API 超时/不可用 → 全部标记为 `"general"`
- 使用现有的 `OPENROUTER_API_KEY` 和 model 配置

#### 3. 采集流程变更

```
Before:  fetch all sources → save to DB
After:   fetch all sources → classifier.classify(items) → save to DB (with category)
```

- `collector.ts` 的 `saveItems()` upsert 中 create/update 都写入 `category`
- 分类在内存中完成，不额外查 DB

#### 4. API 层变更

```diff
// src/routes/trends.ts
- const AI_SOURCES = ['github', 'huggingface', 'hackernews', 'twitter', 'bingnews']
- const GENERAL_SOURCES = ['google', 'reddit', 'duckduckgo', 'v2ex', 'bilibili']
- if (category === 'ai') where.source = { in: AI_SOURCES }
+ if (category) where.category = category
```

- `GET /api/trends?category=ai` → 查 DB 的 `category` 字段
- Source filter 不再受板块限制，任一板块可出现任一来源

#### 5. 前端变更

- 删除 `loadSources()` 中的 `aiSources` / `generalSources` 硬编码过滤
- 两个 tab 的 source filter 显示该分类中**实际存在**的数据源
- 数据渲染逻辑不变

### 涉及文件

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `prisma/schema.prisma` | 修改 | TrendItem 加 category 字段 + CategoryRule model |
| `src/services/classifier.ts` | **新建** | 混合分类引擎 |
| `src/services/collector.ts` | 修改 | collectAll 集成 classifier，saveItems 写入 category |
| `src/routes/trends.ts` | 修改 | 删除硬编码源数组，改查 category 字段 |
| `public/app.js` | 修改 | 删除 source-based 板块过滤 |

### V2 扩展点

- `CategoryRule` 开放前端管理 UI，用户可创建自定义板块
- `classifier.ts` 从 DB 加载 `CategoryRule.keywords`，支持多板块
- 新增 `GET /api/trends/categories` 返回板块列表，前端动态渲染 tab
- 支持一条内容归属多个板块（多对多关系）

---

## V1.1 — 信息源扩展 + 质量优化

**日期**：2026-03-28 ~ 2026-03-29  
**状态**：已完成

### 背景

V1.0 完成基础功能后存在以下问题：
- DuckDuckGo 搜索结果包含大量 Wikipedia 垃圾内容
- Reddit 抓取 r/popular 导致内容与 AI/技术无关
- 无中文信息源，缺少 DeepSeek、千问等国内 AI 覆盖
- Google Trends 链接全部指向 RSS feed URL，无法打开
- Twitter API 频繁 429 限流
- 缺少 publishedAt 时间字段，无法区分新旧内容

### 变更内容

#### 新增 5 个数据源

| 数据源 | 文件 | 采集方式 | 内容类型 |
|--------|------|---------|---------|
| GitHub Trending | `src/sources/github.ts` | HTML 爬取 github.com/trending | 每日热门仓库（stars、语言） |
| HuggingFace | `src/sources/huggingface.ts` | REST API | 热门模型 + 每日论文 |
| V2EX | `src/sources/v2ex.ts` | JSON API | 中文技术社区热帖 |
| Bing News | `src/sources/bingnews.ts` | RSS 爬取 | AI 相关新闻（含 DeepSeek/Qwen） |
| Bilibili | `src/sources/bilibili.ts` | JSON API | B站热搜 + 科技区排行 |

#### 已有数据源优化

| 数据源 | 优化内容 |
|--------|---------|
| Reddit | 从 r/popular 切换到 11 个 AI 子版块（r/LocalLLaMA, r/MachineLearning 等）；增加 score≥50, comments≥5 过滤 |
| HackerNews | 从 Firebase topstories（30 次请求）切换到 Algolia API（单次请求）；增加 points≥20 过滤 |
| DuckDuckGo | 屏蔽 Wikipedia/Britannica URL；关键词改为 AI 聚焦 |
| Twitter | 从 3 个查询循环改为每周期随机 1 个查询（解决 429）；增加互动过滤（likes≥100, retweets≥50） |
| Google Trends | 提取 `<ht:news_item_url>` 作为链接，回退为 Google 搜索链接；修复 101 条历史错误 URL |

#### 数据模型变更

```diff
model TrendItem {
  // ...
+ publishedAt DateTime?       // 原始发布时间
  // ...
+ @@index([publishedAt])
}
```

#### 前端变更

- 新增 "AI 热点" / "综合热点" / "热点监控" 三 Tab 布局
- 各数据源专属元数据展示（GitHub ⭐/语言、HF 模型类型/likes、B站 播放/弹幕等）
- 新增 6 个数据源标签颜色
- Twitter 推文展示（@作者 + ✓认证 + 互动数据）
- 优先显示 publishedAt 时间

#### Collector 变更

- `saveItems()` upsert 增加 `url` 到 update 块（修复旧数据 URL 不更新问题）
- 注册 10 个数据源（8 固定 + Twitter 按 KEY 条件注册）

---

## V1.0 — 基础系统搭建

**日期**：2026-03-27  
**状态**：已完成

### 功能

- Express 5 + Prisma ORM + SQLite 后端
- 5 个初始数据源：Google Trends、Reddit、HackerNews、DuckDuckGo、Twitter/X
- Socket.IO 实时推送
- node-cron 每 30 分钟自动采集
- 关键词监控模块（添加关键词 → 自动检查 → 告警推送）
- AI 趋势分析（OpenRouter API → Gemini 模型）
- 前端 SPA：热点列表 + 来源筛选 + 分页 + 手动刷新 + AI 分析面板

### 架构

```
Frontend (HTML/CSS/JS + Socket.IO)
  ↕ HTTP REST + WebSocket
Express 5 Server
  ├── API Routes (trends, keywords)
  ├── Socket.IO (real-time push)
  ├── Cron Scheduler (30min cycle)
  ├── Source Collector (5 sources)
  ├── Keyword Monitor
  ├── Analysis Service (OpenRouter)
  └── Prisma ORM → SQLite
```

### 数据模型

- `TrendItem` — 热点条目（title, url, source, score, extra, externalId）
- `FetchLog` — 采集日志
- `TrendAnalysis` — AI 分析结果
- `MonitorKeyword` — 监控关键词
- `KeywordAlert` — 关键词告警
- `KeywordTrend` — 关键词热度趋势

### 技术栈

| 模块 | 选型 |
|------|------|
| 后端 | Express 5.x + TypeScript |
| 数据库 | Prisma ORM + SQLite |
| 实时通信 | Socket.IO 4.x |
| 定时任务 | node-cron (CronJob) |
| 爬虫解析 | cheerio + 原生 fetch |
| AI 分析 | OpenRouter API (Gemini) |
| Twitter | twitterapi.io (REST) |
| 开发工具 | tsx watch 热重载 |
